# i18n — App Language Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Spanish/English i18n to tulia.study using react-i18next, with locale stored in useUIStore and a language toggle in Settings.

**Architecture:** `react-i18next` singleton initialized in `src/lib/i18n.ts` with two statically-imported JSON locale files. Locale preference lives in `useUIStore` (same pattern as theme/fontSize), written to `localStorage` and auto-detected from `navigator.language` on first visit. Every visible UI string is replaced with `t()` calls.

**Tech Stack:** React 18, TypeScript 5, Zustand 5, i18next, react-i18next, Vite

---

## File Map

**New files:**
- `src/locales/en.json` — English strings (TypeScript type source of truth)
- `src/locales/es.json` — Spanish strings
- `src/lib/i18n.ts` — i18next singleton init
- `src/types/i18next.d.ts` — module augmentation for typed `t()` keys

**Modified files:**
- `src/main.tsx` — import i18n before render
- `src/lib/store/useUIStore.ts` — add `locale`, `setLocale`
- `src/components/ui/SettingsModal.tsx` — Language row + translate all strings
- `src/components/auth/AuthModal.tsx`
- `src/components/reading/CommentaryPanel.tsx`
- `src/components/reading/CrossReferencesPanel.tsx`
- `src/components/reading/CompareVersionsModal.tsx`
- `src/components/reading/ReadingToolbar.tsx`
- `src/components/sidebar/FavoritesPanel.tsx`
- `src/components/sidebar/MyNotesPanel.tsx`
- `src/components/friends/FriendsPanel.tsx`
- `src/components/friends/FriendSearch.tsx`
- `src/components/friends/FriendRequestCard.tsx`
- `src/components/notes/NoteInput.tsx`
- `src/components/notes/NoteEditor.tsx`
- `src/components/notes/NoteItem.tsx`
- `src/components/study/StudyPanel.tsx`
- `src/components/study/HighlightToolbar.tsx`
- `src/components/ui/CommandPalette.tsx`
- `src/components/ui/KeyboardShortcutsPanel.tsx`
- `src/components/chat/ChatPanel.tsx`

---

## Task 1: Install packages and create locale + type files

**Files:**
- Modify: `package.json` (via pnpm)
- Create: `src/locales/en.json`
- Create: `src/locales/es.json`
- Create: `src/types/i18next.d.ts`
- Create: `src/lib/i18n.ts`

- [ ] **Step 1: Install i18next and react-i18next**

Run in PowerShell (not Git Bash — pnpm PATH):
```
pnpm add i18next react-i18next
```
Expected: both packages added to `dependencies` in `package.json`.

- [ ] **Step 2: Create `src/locales/en.json`**

```json
{
  "settings.title": "Settings",
  "settings.appearance": "Appearance",
  "settings.theme": "Theme",
  "settings.theme.dark": "Dark",
  "settings.theme.light": "Light",
  "settings.fontSize": "Font size",
  "settings.language": "Language",
  "settings.bible": "Bible",
  "settings.bible.version": "Version",
  "settings.signOut": "Sign out",
  "settings.notSignedIn": "Not signed in",

  "nav.chat": "Chat",
  "nav.favorites": "Favorites",
  "nav.myNotes": "My Notes",
  "nav.friends": "Friends",
  "nav.study": "Study",

  "auth.signIn": "Sign in",
  "auth.signInTitle": "Sign in to Tulia",
  "auth.welcomeBack": "Welcome back.",
  "auth.createAccount": "Create account",
  "auth.startJourney": "Start your study journey.",
  "auth.register": "Register",
  "auth.name": "Name",
  "auth.namePlaceholder": "Your name",
  "auth.email": "Email",
  "auth.password": "Password",
  "auth.signingIn": "Signing in…",
  "auth.creatingAccount": "Creating account…",
  "auth.somethingWentWrong": "Something went wrong",
  "auth.logIn": "Log in",

  "commentary.title": "Commentary",
  "commentary.loading": "Loading…",
  "commentary.empty": "No commentary available for this chapter.",

  "notes.placeholder": "Add a note…",
  "notes.placeholderDesktop": "Add a note… ({{modKey}}↵ to save)",
  "notes.editorPlaceholder": "Write your note…",
  "notes.addNote": "Add Note",
  "notes.saving": "Saving…",
  "notes.saved": "Note saved",
  "notes.saveFailed": "Failed to save note",
  "notes.save": "Save",
  "notes.cancel": "Cancel",
  "notes.edit": "Edit",
  "notes.delete": "Delete",
  "notes.confirmDelete": "Delete?",
  "notes.deleteYes": "Yes",
  "notes.deleteNo": "No",
  "notes.updateFailed": "Failed to update note",
  "notes.deleteFailed": "Failed to delete note",
  "notes.empty": "No notes yet.",
  "notes.signInPrompt": "Sign in to see your notes",

  "study.title": "Study",
  "study.empty": "Select a verse to begin studying",
  "study.highlightSelection": "Highlight selection",
  "study.highlightEntireVerse": "Highlight entire verse",
  "study.copySelection": "Copy selection",
  "study.copyVerseText": "Copy verse text",
  "study.highlightColor": "Highlight color",
  "study.highlightSaving": "Saving…",
  "study.charsSelected": "{{count}}ch selected",
  "study.loginRequired": "You need to log in to save highlights",
  "study.colorYellow": "Yellow",
  "study.colorBlue": "Blue",
  "study.colorGreen": "Green",

  "toolbar.commentary": "Chapter commentary",
  "toolbar.compareVersions": "Compare versions",
  "toolbar.crossReferences": "Cross-references",
  "toolbar.selectVerseFirst": "Select a verse first",

  "crossRef.title": "Cross-References",
  "crossRef.empty": "No cross-references found",

  "compareVersions.title": "Compare Versions",
  "compareVersions.loadFailed": "Failed to load",
  "compareVersions.notAvailable": "Not available in this version",

  "commandPalette.placeholder": "Search books or verses...",
  "commandPalette.noResults": "No results",
  "commandPalette.books": "Books",
  "commandPalette.verses": "Verses",
  "commandPalette.oldTestament": "OT",
  "commandPalette.newTestament": "NT",
  "commandPalette.chapters": "ch",

  "shortcuts.title": "Keyboard Shortcuts",
  "shortcuts.nextVerse": "Navigate to next verse",
  "shortcuts.prevVerse": "Navigate to previous verse",
  "shortcuts.focusNote": "Focus note input",
  "shortcuts.toggleHighlight": "Toggle highlight mode",
  "shortcuts.openCommandPalette": "Open command palette",
  "shortcuts.togglePanel": "Toggle this panel",
  "shortcuts.closePanel": "Close panel",

  "friends.title": "Friends",
  "friends.addPeople": "Add people",
  "friends.requests": "Requests ({{count}})",
  "friends.sent": "Sent",
  "friends.friendsCount": "Friends ({{count}})",
  "friends.searchPlaceholder": "Search by name or email…",
  "friends.searching": "Searching…",
  "friends.noUsersFound": "No users found",
  "friends.alreadyFriends": "friends",
  "friends.requestSent": "sent",
  "friends.add": "Add",
  "friends.requestSentTo": "Friend request sent to {{name}}",
  "friends.requestFailed": "Failed to send request",
  "friends.requestAccepted": "Friend request accepted",
  "friends.acceptFailed": "Failed to accept request",
  "friends.declineFailed": "Failed to decline request",
  "friends.removed": "{{name}} removed",
  "friends.removeFailed": "Failed to remove friend",
  "friends.empty": "No friends yet. Search above to add people.",
  "friends.accept": "Accept",
  "friends.decline": "Decline",
  "friends.pending": "pending",

  "favorites.signInPrompt": "Sign in to see favorites",
  "favorites.empty": "No favorites yet",

  "toast.bookmarkAdded": "Bookmark added",
  "toast.bookmarkRemoved": "Bookmark removed",
  "toast.highlightAdded": "Highlight added",
  "toast.copied": "Copied",
  "toast.highlightFailed": "Could not save highlight",

  "time.justNow": "Just now",
  "time.minutes_one": "1 minute ago",
  "time.minutes_other": "{{count}} minutes ago",
  "time.hours_one": "1 hour ago",
  "time.hours_other": "{{count}} hours ago",
  "time.yesterday": "Yesterday",
  "time.days_other": "{{count}} days ago",
  "time.months_one": "1 month ago",
  "time.months_other": "{{count}} months ago",
  "time.years_one": "1 year ago",
  "time.years_other": "{{count}} years ago",

  "common.loading": "Loading…",
  "common.close": "Close",
  "common.back": "Back",
  "common.newChat": "New chat",
  "common.undo": "Undo"
}
```

- [ ] **Step 3: Create `src/locales/es.json`**

```json
{
  "settings.title": "Configuración",
  "settings.appearance": "Apariencia",
  "settings.theme": "Tema",
  "settings.theme.dark": "Oscuro",
  "settings.theme.light": "Claro",
  "settings.fontSize": "Tamaño de letra",
  "settings.language": "Idioma",
  "settings.bible": "Biblia",
  "settings.bible.version": "Versión",
  "settings.signOut": "Cerrar sesión",
  "settings.notSignedIn": "No has iniciado sesión",

  "nav.chat": "Chat",
  "nav.favorites": "Favoritos",
  "nav.myNotes": "Mis notas",
  "nav.friends": "Amigos",
  "nav.study": "Estudio",

  "auth.signIn": "Iniciar sesión",
  "auth.signInTitle": "Inicia sesión en Tulia",
  "auth.welcomeBack": "Bienvenido de nuevo.",
  "auth.createAccount": "Crear cuenta",
  "auth.startJourney": "Empieza tu jornada de estudio.",
  "auth.register": "Registrarse",
  "auth.name": "Nombre",
  "auth.namePlaceholder": "Tu nombre",
  "auth.email": "Correo",
  "auth.password": "Contraseña",
  "auth.signingIn": "Iniciando sesión…",
  "auth.creatingAccount": "Creando cuenta…",
  "auth.somethingWentWrong": "Algo salió mal",
  "auth.logIn": "Inicia sesión",

  "commentary.title": "Comentario",
  "commentary.loading": "Cargando…",
  "commentary.empty": "No hay comentario disponible para este capítulo.",

  "notes.placeholder": "Añade una nota…",
  "notes.placeholderDesktop": "Añade una nota… ({{modKey}}↵ para guardar)",
  "notes.editorPlaceholder": "Escribe tu nota…",
  "notes.addNote": "Añadir nota",
  "notes.saving": "Guardando…",
  "notes.saved": "Nota guardada",
  "notes.saveFailed": "No se pudo guardar la nota",
  "notes.save": "Guardar",
  "notes.cancel": "Cancelar",
  "notes.edit": "Editar",
  "notes.delete": "Eliminar",
  "notes.confirmDelete": "¿Eliminar?",
  "notes.deleteYes": "Sí",
  "notes.deleteNo": "No",
  "notes.updateFailed": "No se pudo actualizar la nota",
  "notes.deleteFailed": "No se pudo eliminar la nota",
  "notes.empty": "Sin notas aún.",
  "notes.signInPrompt": "Inicia sesión para ver tus notas",

  "study.title": "Estudio",
  "study.empty": "Selecciona un versículo para estudiar",
  "study.highlightSelection": "Resaltar selección",
  "study.highlightEntireVerse": "Resaltar versículo completo",
  "study.copySelection": "Copiar selección",
  "study.copyVerseText": "Copiar versículo",
  "study.highlightColor": "Color de resaltado",
  "study.highlightSaving": "Guardando…",
  "study.charsSelected": "{{count}} car. seleccionados",
  "study.loginRequired": "Debes iniciar sesión para guardar resaltados",
  "study.colorYellow": "Amarillo",
  "study.colorBlue": "Azul",
  "study.colorGreen": "Verde",

  "toolbar.commentary": "Comentario del capítulo",
  "toolbar.compareVersions": "Comparar versiones",
  "toolbar.crossReferences": "Referencias cruzadas",
  "toolbar.selectVerseFirst": "Selecciona un versículo primero",

  "crossRef.title": "Referencias cruzadas",
  "crossRef.empty": "No se encontraron referencias cruzadas",

  "compareVersions.title": "Comparar versiones",
  "compareVersions.loadFailed": "Error al cargar",
  "compareVersions.notAvailable": "No disponible en esta versión",

  "commandPalette.placeholder": "Busca libros o versículos...",
  "commandPalette.noResults": "Sin resultados",
  "commandPalette.books": "Libros",
  "commandPalette.verses": "Versículos",
  "commandPalette.oldTestament": "AT",
  "commandPalette.newTestament": "NT",
  "commandPalette.chapters": "cap",

  "shortcuts.title": "Atajos de teclado",
  "shortcuts.nextVerse": "Ir al versículo siguiente",
  "shortcuts.prevVerse": "Ir al versículo anterior",
  "shortcuts.focusNote": "Enfocar campo de nota",
  "shortcuts.toggleHighlight": "Activar modo resaltado",
  "shortcuts.openCommandPalette": "Abrir paleta de comandos",
  "shortcuts.togglePanel": "Alternar este panel",
  "shortcuts.closePanel": "Cerrar panel",

  "friends.title": "Amigos",
  "friends.addPeople": "Agregar personas",
  "friends.requests": "Solicitudes ({{count}})",
  "friends.sent": "Enviadas",
  "friends.friendsCount": "Amigos ({{count}})",
  "friends.searchPlaceholder": "Buscar por nombre o correo…",
  "friends.searching": "Buscando…",
  "friends.noUsersFound": "No se encontraron usuarios",
  "friends.alreadyFriends": "amigos",
  "friends.requestSent": "enviado",
  "friends.add": "Agregar",
  "friends.requestSentTo": "Solicitud enviada a {{name}}",
  "friends.requestFailed": "No se pudo enviar la solicitud",
  "friends.requestAccepted": "Solicitud de amistad aceptada",
  "friends.acceptFailed": "No se pudo aceptar la solicitud",
  "friends.declineFailed": "No se pudo rechazar la solicitud",
  "friends.removed": "{{name}} eliminado",
  "friends.removeFailed": "No se pudo eliminar el amigo",
  "friends.empty": "Sin amigos aún. Busca arriba para agregar personas.",
  "friends.accept": "Aceptar",
  "friends.decline": "Rechazar",
  "friends.pending": "pendiente",

  "favorites.signInPrompt": "Inicia sesión para ver favoritos",
  "favorites.empty": "Sin favoritos aún",

  "toast.bookmarkAdded": "Marcador añadido",
  "toast.bookmarkRemoved": "Marcador eliminado",
  "toast.highlightAdded": "Resaltado añadido",
  "toast.copied": "Copiado",
  "toast.highlightFailed": "No se pudo guardar el resaltado",

  "time.justNow": "Hace un momento",
  "time.minutes_one": "Hace 1 minuto",
  "time.minutes_other": "Hace {{count}} minutos",
  "time.hours_one": "Hace 1 hora",
  "time.hours_other": "Hace {{count}} horas",
  "time.yesterday": "Ayer",
  "time.days_other": "Hace {{count}} días",
  "time.months_one": "Hace 1 mes",
  "time.months_other": "Hace {{count}} meses",
  "time.years_one": "Hace 1 año",
  "time.years_other": "Hace {{count}} años",

  "common.loading": "Cargando…",
  "common.close": "Cerrar",
  "common.back": "Atrás",
  "common.newChat": "Nuevo chat",
  "common.undo": "Deshacer"
}
```

- [ ] **Step 4: Create `src/types/i18next.d.ts`**

```ts
import en from '../locales/en.json'

declare module 'i18next' {
  interface CustomTypeOptions {
    defaultNS: 'translation'
    resources: { translation: typeof en }
  }
}
```

- [ ] **Step 5: Create `src/lib/i18n.ts`**

```ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import en from '@/locales/en.json'
import es from '@/locales/es.json'

const savedLocale = localStorage.getItem('locale')
const osLocale   = navigator.language.startsWith('es') ? 'es' : 'en'

i18n.use(initReactI18next).init({
  lng: savedLocale ?? osLocale,
  fallbackLng: 'en',
  resources: {
    en: { translation: en },
    es: { translation: es },
  },
  interpolation: { escapeValue: false },
})

export default i18n
```

- [ ] **Step 6: Write test for i18n init**

Create `src/lib/__tests__/i18n.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'

describe('i18n initialization', () => {
  beforeEach(() => {
    localStorage.clear()
  })

  it('defaults to en when no locale saved and navigator.language is en', async () => {
    Object.defineProperty(navigator, 'language', { value: 'en-US', configurable: true })
    const { default: i18n } = await import('../i18n')
    expect(i18n.language).toBe('en')
  })

  it('loads the correct English translation for a known key', async () => {
    const { default: i18n } = await import('../i18n')
    expect(i18n.t('settings.title')).toBe('Settings')
  })

  it('loads the correct Spanish translation for a known key', async () => {
    const { default: i18n } = await import('../i18n')
    await i18n.changeLanguage('es')
    expect(i18n.t('settings.title')).toBe('Configuración')
    await i18n.changeLanguage('en')
  })
})
```

- [ ] **Step 7: Run test**

```
pnpm test
```
Expected: 3 passing tests in `i18n.test.ts`.

- [ ] **Step 8: Commit**

```bash
git add src/locales/en.json src/locales/es.json src/lib/i18n.ts src/types/i18next.d.ts src/lib/__tests__/i18n.test.ts package.json pnpm-lock.yaml
git commit -m "feat: add i18n scaffold — locale files, i18next init, TS types"
```

---

## Task 2: Wire i18n into app entry and extend useUIStore

**Files:**
- Modify: `src/main.tsx`
- Modify: `src/lib/store/useUIStore.ts`

- [ ] **Step 1: Update `src/main.tsx`** — add the import before ReactDOM

```ts
import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App'
import './app/globals.css'
import './lib/i18n'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
```

- [ ] **Step 2: Update `src/lib/store/useUIStore.ts`** — add `Locale` type, `locale` state, and `setLocale` action

Add `import i18n from '@/lib/i18n'` at the top.

Add `export type Locale = 'en' | 'es'` alongside the other exported types.

Add to `UIStore` interface:
```ts
locale: Locale
setLocale: (l: Locale) => void
```

Add to initial state (after `savedReadingMode`):
```ts
const savedLocale = (localStorage.getItem('locale') as Locale) ?? null
```

In the store body, add `locale` to initial state — read `savedLocale` if present, otherwise detect from OS (the i18n singleton already handles this, so mirror it):
```ts
locale: (savedLocale ?? (navigator.language.startsWith('es') ? 'es' : 'en')) as Locale,
```

Add action:
```ts
setLocale: (l) => {
  localStorage.setItem('locale', l)
  void i18n.changeLanguage(l)
  set({ locale: l })
},
```

- [ ] **Step 3: Verify TypeScript compiles**

```
pnpm build
```
Expected: no TypeScript errors. (Build may warn about unused locale import in App — that's fine.)

- [ ] **Step 4: Commit**

```bash
git add src/main.tsx src/lib/store/useUIStore.ts
git commit -m "feat: wire i18n into app entry and add locale to useUIStore"
```

---

## Task 3: SettingsModal — Language row and translate all strings

**Files:**
- Modify: `src/components/ui/SettingsModal.tsx`

- [ ] **Step 1: Replace `SettingsModal.tsx` with translated version**

```tsx
import { useEffect } from 'react'
import { useTranslation } from 'react-i18next'
import { useUIStore, type FontSize, type Theme, type Locale } from '@/lib/store/useUIStore'
import { useVerseStore } from '@/lib/store/useVerseStore'
import { useAuthStore } from '@/lib/store/useAuthStore'
import { UserAvatar } from '@/components/auth/UserAvatar'
import { cn } from '@/lib/cn'

const FONT_OPTIONS: { value: FontSize; label: string }[] = [
  { value: 'sm',   label: 'S' },
  { value: 'base', label: 'M' },
  { value: 'lg',   label: 'L' },
]

const LOCALE_OPTIONS: { value: Locale; label: string }[] = [
  { value: 'es', label: 'ES' },
  { value: 'en', label: 'EN' },
]

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div className="flex flex-col gap-3">
      <p className="text-2xs uppercase tracking-wider text-text-muted px-5">{title}</p>
      <div className="flex flex-col">{children}</div>
    </div>
  )
}

function Row({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div className="flex items-center justify-between gap-4 px-5 py-2">
      <span className="text-sm text-text-secondary">{label}</span>
      <div className="shrink-0">{children}</div>
    </div>
  )
}

function SunIcon() {
  return (
    <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.4" className="w-3.5 h-3.5">
      <circle cx="8" cy="8" r="2.5" />
      <path d="M8 1v2M8 13v2M1 8h2M13 8h2M3.05 3.05l1.41 1.41M11.54 11.54l1.41 1.41M3.05 12.95l1.41-1.41M11.54 4.46l1.41-1.41" strokeLinecap="round"/>
    </svg>
  )
}

function MoonIcon() {
  return (
    <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.4" className="w-3.5 h-3.5">
      <path d="M13.5 10A6 6 0 0 1 6 2.5a6 6 0 1 0 7.5 7.5z" strokeLinecap="round" strokeLinejoin="round"/>
    </svg>
  )
}

export function SettingsModal() {
  const { t } = useTranslation()

  const settingsOpen  = useUIStore(s => s.settingsOpen)
  const closeSettings = useUIStore(s => s.closeSettings)
  const fontSize      = useUIStore(s => s.fontSize)
  const setFontSize   = useUIStore(s => s.setFontSize)
  const theme         = useUIStore(s => s.theme)
  const setTheme      = useUIStore(s => s.setTheme)
  const locale        = useUIStore(s => s.locale)
  const setLocale     = useUIStore(s => s.setLocale)

  const versions     = useVerseStore(s => s.versions)
  const versionId    = useVerseStore(s => s.versionId)
  const loadVersions = useVerseStore(s => s.loadVersions)
  const setVersion   = useVerseStore(s => s.setVersion)

  const user   = useAuthStore(s => s.user)
  const logout = useAuthStore(s => s.logout)

  useEffect(() => {
    if (settingsOpen && versions.length === 0) loadVersions()
  }, [settingsOpen])

  if (!settingsOpen) return null

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm"
      onClick={closeSettings}
    >
      <div
        className="w-full max-w-sm bg-bg-secondary border border-border-subtle rounded-xl shadow-2xl mx-4 overflow-hidden"
        onClick={e => e.stopPropagation()}
      >
        {/* Profile header */}
        <div className="px-5 py-5 border-b border-border-subtle">
          {user ? (
            <div className="flex items-center gap-3">
              <UserAvatar email={user.email} size="md" />
              <div className="flex-1 min-w-0">
                <p className="text-sm font-medium text-text-primary truncate">{user.name}</p>
                <p className="text-xs text-text-muted truncate">{user.email}</p>
              </div>
              <button
                onClick={closeSettings}
                className="text-text-muted hover:text-text-primary transition-colors text-xl leading-none ml-1"
              >
                ×
              </button>
            </div>
          ) : (
            <div className="flex items-center justify-between">
              <p className="text-sm text-text-muted">{t('settings.notSignedIn')}</p>
              <button
                onClick={closeSettings}
                className="text-text-muted hover:text-text-primary transition-colors text-xl leading-none"
              >
                ×
              </button>
            </div>
          )}
        </div>

        {/* Settings body */}
        <div className="py-4 flex flex-col gap-5">

          {/* Appearance */}
          <Section title={t('settings.appearance')}>
            <Row label={t('settings.theme')}>
              <div className="flex gap-1 bg-bg-tertiary rounded-lg p-1">
                {(['dark', 'light'] as Theme[]).map(th => (
                  <button
                    key={th}
                    onClick={() => setTheme(th)}
                    className={cn(
                      'flex items-center gap-1.5 px-3 py-1.5 rounded-md text-xs font-medium transition-colors',
                      theme === th
                        ? 'bg-bg-secondary text-text-primary shadow-sm'
                        : 'text-text-muted hover:text-text-secondary'
                    )}
                  >
                    {th === 'dark' ? <MoonIcon /> : <SunIcon />}
                    {th === 'dark' ? t('settings.theme.dark') : t('settings.theme.light')}
                  </button>
                ))}
              </div>
            </Row>

            <Row label={t('settings.language')}>
              <div className="flex gap-1 bg-bg-tertiary rounded-lg p-1">
                {LOCALE_OPTIONS.map(opt => (
                  <button
                    key={opt.value}
                    onClick={() => setLocale(opt.value)}
                    className={cn(
                      'px-3 py-1.5 rounded-md text-xs font-medium transition-colors',
                      locale === opt.value
                        ? 'bg-bg-secondary text-text-primary shadow-sm'
                        : 'text-text-muted hover:text-text-secondary'
                    )}
                  >
                    {opt.label}
                  </button>
                ))}
              </div>
            </Row>

            <Row label={t('settings.fontSize')}>
              <div className="flex gap-1">
                {FONT_OPTIONS.map(opt => (
                  <button
                    key={opt.value}
                    onClick={() => setFontSize(opt.value)}
                    className={cn(
                      'w-8 h-8 rounded-lg text-sm border font-medium transition-colors',
                      fontSize === opt.value
                        ? 'bg-accent/20 border-accent/40 text-accent'
                        : 'bg-bg-tertiary border-border-subtle text-text-secondary hover:text-text-primary',
                    )}
                  >
                    {opt.label}
                  </button>
                ))}
              </div>
            </Row>
          </Section>

          {/* Bible */}
          <Section title={t('settings.bible')}>
            <Row label={t('settings.bible.version')}>
              <select
                value={versionId}
                onChange={e => setVersion(Number(e.target.value))}
                className={cn(
                  'bg-bg-tertiary border border-border-subtle rounded-lg px-3 py-1.5',
                  'text-sm text-text-primary outline-none focus:border-accent/50 transition-colors cursor-pointer',
                )}
              >
                {versions.length === 0 && <option value={versionId}>{t('common.loading')}</option>}
                {versions.map(v => (
                  <option key={v.id} value={v.id}>
                    {v.abbreviation} — {v.name}
                  </option>
                ))}
              </select>
            </Row>
          </Section>

          {/* Account actions */}
          {user && (
            <div className="px-5 pt-1 border-t border-border-subtle mt-1">
              <button
                onClick={() => { logout(); closeSettings() }}
                className="mt-3 text-sm text-red-400 hover:text-red-300 transition-colors"
              >
                {t('settings.signOut')}
              </button>
            </div>
          )}

        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Start dev server and verify Settings modal renders correctly**

```
pnpm dev
```
Open Settings. Confirm:
- Language row appears between Theme and Font size with ES/EN buttons
- Switching language updates all labels in the modal immediately
- Selection persists across page refresh

- [ ] **Step 3: Commit**

```bash
git add src/components/ui/SettingsModal.tsx
git commit -m "feat: add language selector to Settings and translate all settings strings"
```

---

## Task 4: Translate AuthModal

**Files:**
- Modify: `src/components/auth/AuthModal.tsx`

- [ ] **Step 1: Update `AuthModal.tsx`**

```tsx
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { useAuthStore } from '@/lib/store/useAuthStore'
import { cn } from '@/lib/cn'

interface AuthModalProps {
  open: boolean
  onClose: () => void
}

type Mode = 'login' | 'register'

export function AuthModal({ open, onClose }: AuthModalProps) {
  const { t } = useTranslation()
  const [mode, setMode] = useState<Mode>('login')
  const [name, setName] = useState('')
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState('')
  const [loading, setLoading] = useState(false)

  const login = useAuthStore(s => s.login)
  const register = useAuthStore(s => s.register)

  if (!open) return null

  const reset = () => {
    setName(''); setEmail(''); setPassword(''); setError(''); setLoading(false)
  }

  const handleClose = () => {
    onClose()
    setTimeout(reset, 200)
  }

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    setError('')
    setLoading(true)
    try {
      if (mode === 'login') {
        await login(email.trim(), password)
      } else {
        await register(name.trim(), email.trim(), password)
      }
      handleClose()
    } catch (err) {
      setError(err instanceof Error ? err.message : t('auth.somethingWentWrong'))
    } finally {
      setLoading(false)
    }
  }

  const switchMode = (m: Mode) => { setMode(m); setError('') }

  return (
    <div
      className="fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-sm"
      onClick={handleClose}
    >
      <div
        className="max-w-sm w-full bg-bg-secondary rounded-xl border border-border-subtle shadow-2xl p-6 mx-4"
        onClick={e => e.stopPropagation()}
      >
        {/* Header */}
        <div className="flex items-start justify-between mb-5">
          <div>
            <h2 className="text-md font-medium text-text-primary">
              {mode === 'login' ? t('auth.signInTitle') : t('auth.createAccount')}
            </h2>
            <p className="text-sm text-text-muted mt-0.5">
              {mode === 'login' ? t('auth.welcomeBack') : t('auth.startJourney')}
            </p>
          </div>
          <button
            onClick={handleClose}
            className="text-text-muted hover:text-text-secondary transition-colors text-lg leading-none ml-4 mt-0.5"
          >
            ×
          </button>
        </div>

        {/* Mode tabs */}
        <div className="flex gap-1 mb-4 bg-bg-tertiary rounded-lg p-1">
          {(['login', 'register'] as Mode[]).map(m => (
            <button
              key={m}
              onClick={() => switchMode(m)}
              className={cn(
                'flex-1 py-1.5 rounded-md text-sm font-medium transition-colors',
                mode === m
                  ? 'bg-bg-secondary text-text-primary shadow-sm'
                  : 'text-text-muted hover:text-text-secondary'
              )}
            >
              {m === 'login' ? t('auth.signIn') : t('auth.register')}
            </button>
          ))}
        </div>

        <form onSubmit={handleSubmit} className="flex flex-col gap-3" noValidate>
          {mode === 'register' && (
            <div className="flex flex-col gap-1.5">
              <label htmlFor="auth-name" className="text-sm text-text-secondary">{t('auth.name')}</label>
              <input
                id="auth-name"
                type="text"
                value={name}
                onChange={e => setName(e.target.value)}
                placeholder={t('auth.namePlaceholder')}
                autoComplete="name"
                autoFocus
                className={inputCls}
              />
            </div>
          )}

          <div className="flex flex-col gap-1.5">
            <label htmlFor="auth-email" className="text-sm text-text-secondary">{t('auth.email')}</label>
            <input
              id="auth-email"
              type="email"
              value={email}
              onChange={e => { setEmail(e.target.value); setError('') }}
              placeholder="you@example.com"
              autoComplete="email"
              autoFocus={mode === 'login'}
              className={inputCls}
            />
          </div>

          <div className="flex flex-col gap-1.5">
            <label htmlFor="auth-password" className="text-sm text-text-secondary">{t('auth.password')}</label>
            <input
              id="auth-password"
              type="password"
              value={password}
              onChange={e => { setPassword(e.target.value); setError('') }}
              placeholder="••••••••"
              autoComplete={mode === 'login' ? 'current-password' : 'new-password'}
              className={inputCls}
            />
          </div>

          {error && <p className="text-2xs text-red-400">{error}</p>}

          <button
            type="submit"
            disabled={loading || !email.trim() || !password}
            className={cn(
              'w-full bg-accent text-bg-primary font-medium rounded-lg py-2.5 text-sm mt-1',
              'transition-opacity',
              (loading || !email.trim() || !password) ? 'opacity-50 cursor-not-allowed' : 'hover:opacity-90'
            )}
          >
            {loading
              ? (mode === 'login' ? t('auth.signingIn') : t('auth.creatingAccount'))
              : (mode === 'login' ? t('auth.signIn') : t('auth.createAccount'))}
          </button>
        </form>
      </div>
    </div>
  )
}

const inputCls = cn(
  'w-full bg-bg-tertiary border border-border-subtle rounded-lg px-3 py-2.5',
  'text-sm text-text-primary placeholder:text-text-muted',
  'outline-none focus:border-accent/50 transition-colors'
)
```

- [ ] **Step 2: Verify in browser** — open AuthModal in both languages, confirm all labels translate.

- [ ] **Step 3: Commit**

```bash
git add src/components/auth/AuthModal.tsx
git commit -m "feat(i18n): translate AuthModal"
```

---

## Task 5: Translate reading panels (Commentary, CrossRef, CompareVersions, ReadingToolbar)

**Files:**
- Modify: `src/components/reading/CommentaryPanel.tsx`
- Modify: `src/components/reading/CrossReferencesPanel.tsx`
- Modify: `src/components/reading/CompareVersionsModal.tsx`
- Modify: `src/components/reading/ReadingToolbar.tsx`

- [ ] **Step 1: Update `CommentaryPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Commentary` → `{t('commentary.title')}`
- `Loading…` (in the loading div) → `{t('commentary.loading')}`
- `No commentary available for this chapter.` → `{t('commentary.empty')}`

- [ ] **Step 2: Update `CrossReferencesPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Cross-References` → `{t('crossRef.title')}`
- `Loading…` → `{t('common.loading')}`
- `No cross-references found` → `{t('crossRef.empty')}`

- [ ] **Step 3: Update `CompareVersionsModal.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Compare Versions` → `{t('compareVersions.title')}`
- `Loading…` → `{t('common.loading')}`
- `Failed to load` → `{t('compareVersions.loadFailed')}`
- `Not available in this version` → `{t('compareVersions.notAvailable')}`

- [ ] **Step 4: Update `ReadingToolbar.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace Tooltip labels:
- `"Chapter commentary"` → `{t('toolbar.commentary')}`
- `"Compare versions"` → `{t('toolbar.compareVersions')}`
- `selectedVerse ? 'Cross-references' : 'Select a verse first'` → `selectedVerse ? t('toolbar.crossReferences') : t('toolbar.selectVerseFirst')`

- [ ] **Step 5: Commit**

```bash
git add src/components/reading/CommentaryPanel.tsx src/components/reading/CrossReferencesPanel.tsx src/components/reading/CompareVersionsModal.tsx src/components/reading/ReadingToolbar.tsx
git commit -m "feat(i18n): translate reading panels"
```

---

## Task 6: Translate sidebar panels (Favorites, MyNotes)

**Files:**
- Modify: `src/components/sidebar/FavoritesPanel.tsx`
- Modify: `src/components/sidebar/MyNotesPanel.tsx`

- [ ] **Step 1: Update `FavoritesPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Sign in to see favorites` → `{t('favorites.signInPrompt')}`
- `Favorites` (header) → `{t('nav.favorites')}`
- `Loading…` → `{t('common.loading')}`
- `No favorites yet` → `{t('favorites.empty')}`

- [ ] **Step 2: Update `MyNotesPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Sign in to see your notes` → `{t('notes.signInPrompt')}`
- `My Notes` (header) → `{t('nav.myNotes')}`
- `Loading…` → `{t('common.loading')}`
- `No notes yet` → `{t('notes.empty')}`

- [ ] **Step 3: Commit**

```bash
git add src/components/sidebar/FavoritesPanel.tsx src/components/sidebar/MyNotesPanel.tsx
git commit -m "feat(i18n): translate sidebar panels"
```

---

## Task 7: Translate Friends components

**Files:**
- Modify: `src/components/friends/FriendsPanel.tsx`
- Modify: `src/components/friends/FriendSearch.tsx`
- Modify: `src/components/friends/FriendRequestCard.tsx`

- [ ] **Step 1: Update `FriendsPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings and toast calls:
- Header `Friends` → `{t('friends.title')}`
- `Add people` → `{t('friends.addPeople')}`
- `` `Requests (${received.length})` `` → `{t('friends.requests', { count: received.length })}`
- `Sent` → `{t('friends.sent')}`
- `` `Friends ${friends.length > 0 ? \`(${friends.length})\` : ''}` `` → `{friends.length > 0 ? t('friends.friendsCount', { count: friends.length }) : t('friends.title')}`
- `No friends yet. Search above to add people.` → `{t('friends.empty')}`
- Toast `'Friend request accepted'` → `t('friends.requestAccepted')`
- Toast `'Failed to accept request'` → `t('friends.acceptFailed')`
- Toast `'Failed to decline request'` → `t('friends.declineFailed')`
- Toast `` `${friend.name} removed` `` → `t('friends.removed', { name: friend.name })`
- Toast action label `'Undo'` → `t('common.undo')`
- Toast `'Failed to remove friend'` → `t('friends.removeFailed')`

- [ ] **Step 2: Update `FriendSearch.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `placeholder="Search by name or email…"` → `placeholder={t('friends.searchPlaceholder')}`
- `Searching…` → `{t('friends.searching')}`
- `friends` (status span) → `{t('friends.alreadyFriends')}`
- `sent` (status span) → `{t('friends.requestSent')}`
- `Add` (button) → `{t('friends.add')}`
- `` `Friend request sent to ${userName}` `` (toast) → `t('friends.requestSentTo', { name: userName })`
- `'Failed to send request'` (toast) → `t('friends.requestFailed')`
- `No users found` → `{t('friends.noUsersFound')}`

- [ ] **Step 3: Update `FriendRequestCard.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Accept` → `{t('friends.accept')}`
- `Decline` → `{t('friends.decline')}`
- `pending` → `{t('friends.pending')}`

- [ ] **Step 4: Commit**

```bash
git add src/components/friends/FriendsPanel.tsx src/components/friends/FriendSearch.tsx src/components/friends/FriendRequestCard.tsx
git commit -m "feat(i18n): translate Friends components"
```

---

## Task 8: Translate Notes components

**Files:**
- Modify: `src/components/notes/NoteInput.tsx`
- Modify: `src/components/notes/NoteEditor.tsx`
- Modify: `src/components/notes/NoteItem.tsx`

- [ ] **Step 1: Update `NoteInput.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- Mobile placeholder `'Add a note…'` → `t('notes.placeholder')`
- Desktop placeholder `` `Add a note… (${modKey}↵ to save)` `` → `t('notes.placeholderDesktop', { modKey })`
- `'Saving…'` → `t('notes.saving')`
- `'Add Note'` → `t('notes.addNote')`
- Toast `'Note saved'` → `t('notes.saved')`
- Toast `'Failed to save note'` → `t('notes.saveFailed')`

- [ ] **Step 2: Update `NoteEditor.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `placeholder="Write your note…"` → `placeholder={t('notes.editorPlaceholder')}`
- `Cancel` → `{t('notes.cancel')}`
- `Save` → `{t('notes.save')}`

- [ ] **Step 3: Update `NoteItem.tsx`**

The `formatRelativeTime` function must be moved inside the component so it can call `t()`. Remove the standalone function and replace with an inline helper.

Add `import { useTranslation } from 'react-i18next'` at the top.

Inside `NoteItem`:
```tsx
const { t } = useTranslation()

function formatRelativeTime(isoString: string): string {
  const now = Date.now()
  const diffMs   = now - new Date(isoString).getTime()
  const diffSecs = Math.floor(diffMs / 1000)
  const diffMins = Math.floor(diffSecs / 60)
  const diffHrs  = Math.floor(diffMins / 60)
  const diffDays = Math.floor(diffHrs / 24)

  if (diffSecs < 60)  return t('time.justNow')
  if (diffMins < 60)  return t('time.minutes', { count: diffMins })
  if (diffHrs  < 24)  return t('time.hours',   { count: diffHrs })
  if (diffDays === 1) return t('time.yesterday')
  if (diffDays < 30)  return t('time.days',    { count: diffDays })
  const diffMonths = Math.floor(diffDays / 30)
  if (diffMonths < 12) return t('time.months', { count: diffMonths })
  return t('time.years', { count: Math.floor(diffMonths / 12) })
}
```

Replace confirm-delete UI strings:
- `Delete?` → `{t('notes.confirmDelete')}`
- `Yes` → `{t('notes.deleteYes')}`
- `No` → `{t('notes.deleteNo')}`
- `Edit` → `{t('notes.edit')}`
- `Delete` → `{t('notes.delete')}`

Replace toast calls:
- `'Failed to update note'` → `t('notes.updateFailed')`
- `'Note deleted'` → `t('notes.delete')` — wait, this is a toast message. Use `t('notes.deleteFailed')` for the error and `t('notes.delete')` only for the button. The `addToast('Note deleted', 'info')` should become `addToast(t('notes.delete') + 'd', ...)` — actually use a dedicated key. The spec has `"toast.noteDeleted"` key already not added to locales. Add it now:

Add to `en.json`: `"toast.noteDeleted": "Note deleted"`
Add to `es.json`: `"toast.noteDeleted": "Nota eliminada"`

Then use `t('toast.noteDeleted')` in the toast call.

- [ ] **Step 4: Add missing toast keys to locale files**

In `src/locales/en.json` add (if not already present):
```json
"toast.noteDeleted": "Note deleted"
```

In `src/locales/es.json` add:
```json
"toast.noteDeleted": "Nota eliminada"
```

- [ ] **Step 5: Commit**

```bash
git add src/components/notes/NoteInput.tsx src/components/notes/NoteEditor.tsx src/components/notes/NoteItem.tsx src/locales/en.json src/locales/es.json
git commit -m "feat(i18n): translate Notes components"
```

---

## Task 9: Translate Study panel and HighlightToolbar

**Files:**
- Modify: `src/components/study/StudyPanel.tsx`
- Modify: `src/components/study/HighlightToolbar.tsx`

- [ ] **Step 1: Update `StudyPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

The `HIGHLIGHT_COLORS` constant is at module level with hardcoded labels. Move it inside the component body, replacing the `label` values with `t()` calls:

```tsx
const HIGHLIGHT_COLORS: { label: string; value: HighlightColor; hex: string }[] = [
  { label: t('study.colorYellow'), value: 'yellow', hex: '#e5c07b' },
  { label: t('study.colorBlue'),   value: 'blue',   hex: '#61afef' },
  { label: t('study.colorGreen'),  value: 'green',  hex: '#98c379' },
]
```

Replace strings in the component body and context menu items:
- `'Highlight selection'` (label) → `t('study.highlightSelection')`
- `'Highlight entire verse'` (label) → `t('study.highlightEntireVerse')`
- `'Copy selection'` → `t('study.copySelection')`
- `'Copy verse text'` → `t('study.copyVerseText')`
- `'Study'` (panel title fallback) → `t('study.title')`
- `'Select a verse to begin studying'` → `t('study.empty')`
- Toast `'Highlight added'` → `t('toast.highlightAdded')`
- Toast `'Copied'` → `t('toast.copied')`
- Toast `'You need to log in to save highlights'` → `t('study.loginRequired')`
- Toast action `'Log in'` → `t('auth.logIn')`
- Toast `'Could not save highlight'` → `t('toast.highlightFailed')`

- [ ] **Step 2: Update `HighlightToolbar.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `aria-label="Highlight color"` → `aria-label={t('study.highlightColor')}`
- title `` `${value.charAt(0).toUpperCase() + value.slice(1)} highlight` `` — replace with a map using translation keys: build a helper inside the component:
  ```tsx
  const colorLabel: Record<HighlightColor, string> = {
    yellow: t('study.colorYellow'),
    blue:   t('study.colorBlue'),
    green:  t('study.colorGreen'),
  }
  ```
  Then use `title={colorLabel[value]}` in the button.
- `'Saving…'` → `t('study.highlightSaving')`
- `'Highlight selection'` (button) → `t('study.highlightSelection')`
- `` `${selectionRange.end - selectionRange.start}ch selected` `` → `t('study.charsSelected', { count: selectionRange.end - selectionRange.start })`
- Toast `'Highlight added'` → `t('toast.highlightAdded')`
- Toast `'You need to log in to save highlights'` → `t('study.loginRequired')`
- Toast action `'Log in'` → `t('auth.logIn')`
- Toast `'Could not save highlight'` → `t('toast.highlightFailed')`

- [ ] **Step 3: Commit**

```bash
git add src/components/study/StudyPanel.tsx src/components/study/HighlightToolbar.tsx
git commit -m "feat(i18n): translate Study panel and HighlightToolbar"
```

---

## Task 10: Translate global UI (CommandPalette, KeyboardShortcutsPanel, ChatPanel)

**Files:**
- Modify: `src/components/ui/CommandPalette.tsx`
- Modify: `src/components/ui/KeyboardShortcutsPanel.tsx`
- Modify: `src/components/chat/ChatPanel.tsx`

- [ ] **Step 1: Update `CommandPalette.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `placeholder="Search books or verses..."` → `placeholder={t('commandPalette.placeholder')}`
- `No results` → `{t('commandPalette.noResults')}`
- `Books` (group heading span) → `{t('commandPalette.books')}`
- `Verses` (group heading span) → `{t('commandPalette.verses')}`
- `book.testament === 'old' ? 'OT' : 'NT'` → `book.testament === 'old' ? t('commandPalette.oldTestament') : t('commandPalette.newTestament')`
- `` `${book.chapters} ch` `` → `` `${book.chapters} ${t('commandPalette.chapters')}` ``

- [ ] **Step 2: Update `KeyboardShortcutsPanel.tsx`**

The `SHORTCUTS` constant is at module level with hardcoded description strings. Move it inside the component so descriptions can use `t()`.

Add `import { useTranslation } from 'react-i18next'` at the top (remove the unused existing blank lines).

Replace the module-level `SHORTCUTS` with a computed value inside the component:

```tsx
export function KeyboardShortcutsPanel() {
  const { t } = useTranslation()
  const { shortcutsPanelOpen, toggleShortcutsPanel } = useUIStore()
  const isMobile = useIsMobile()

  const SHORTCUTS = [
    { key: 'J',          description: t('shortcuts.nextVerse') },
    { key: 'K',          description: t('shortcuts.prevVerse') },
    { key: 'N',          description: t('shortcuts.focusNote') },
    { key: 'H',          description: t('shortcuts.toggleHighlight') },
    { key: `${modKey}K`, description: t('shortcuts.openCommandPalette') },
    { key: '?',          description: t('shortcuts.togglePanel') },
    { key: 'Esc',        description: t('shortcuts.closePanel') },
  ]
  // ... rest of component unchanged except:
  // "Keyboard Shortcuts" → {t('shortcuts.title')}
```

Replace the header title:
- `Keyboard Shortcuts` → `{t('shortcuts.title')}`

- [ ] **Step 3: Update `ChatPanel.tsx`**

Add `import { useTranslation } from 'react-i18next'` and `const { t } = useTranslation()` inside the component.

Replace strings:
- `Chat` (header span) → `{t('nav.chat')}`
- `aria-label="New chat"` → `aria-label={t('common.newChat')}`
- `title="New chat"` → `title={t('common.newChat')}`

- [ ] **Step 4: Verify full coverage in browser**

Start the dev server (`pnpm dev`), switch to Spanish. Go through every panel and modal:
- [ ] Settings shows in Spanish
- [ ] Auth modal shows in Spanish
- [ ] Commentary panel header shows in Spanish
- [ ] CrossRef panel shows in Spanish
- [ ] Compare versions modal shows in Spanish
- [ ] Reading toolbar tooltips show in Spanish
- [ ] Favorites panel shows in Spanish
- [ ] My Notes panel shows in Spanish
- [ ] Friends panel shows in Spanish
- [ ] Notes input placeholder shows in Spanish
- [ ] Highlight toolbar button shows in Spanish
- [ ] Command palette placeholder shows in Spanish
- [ ] Keyboard shortcuts panel shows in Spanish
- [ ] Chat header shows in Spanish

Switch back to English and verify all strings revert.

- [ ] **Step 5: Commit**

```bash
git add src/components/ui/CommandPalette.tsx src/components/ui/KeyboardShortcutsPanel.tsx src/components/chat/ChatPanel.tsx
git commit -m "feat(i18n): translate CommandPalette, KeyboardShortcutsPanel, ChatPanel"
```

- [ ] **Step 6: Final cleanup commit**

```bash
git add -A
git commit -m "feat: complete i18n — Spanish and English UI language support"
```
